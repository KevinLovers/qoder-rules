---
trigger: manual
---

# MFQ-M 单功能生成规范 v1.0
# ============================================
# 基于需求场景生成单功能测试用例的规则和要求
# 严格遵循 TCO（Test Case Optimization）原则
#
# 使用方法：
# 1. 将此文件放在项目根目录
# 2. 在 AI 对话中使用 @MFQ-M-single-function-spec.zh-CN.md 引用
# 3. AI 将自动遵循这些规则生成单功能测试用例
#
# 输入来源：需求画场景.md
# 输出格式：MFQ-M 单功能测试用例
# 优化原则：TCO（Test Case Optimization）
# 最后更新：2025-01-XX
# ============================================

## [规则 1] 从需求画场景.md读取场景（优先级：关键）
# 必须从需求画场景.md文件中读取场景信息

说明：
- 读取当前目录下的 `需求画场景.md` 文件
- 解析文件中的所有场景（格式：`### 场景N：`）
- 提取每个场景的**场景描述**和**验收准则**（Given|When|Then表格）
- 如果文件不存在，提示用户先执行需求提取流程

工作流程：
1. 检查 `需求画场景.md` 文件是否存在
2. 如果不存在，提示："需求画场景.md 文件不存在，请先执行需求提取流程"
3. 如果存在，解析文件中的所有场景
4. 提取每个场景的完整信息（场景编号、描述、验收准则）

后果：
- 未从正确来源读取会导致测试用例与需求不匹配
- 遗漏场景会导致测试覆盖率不足

示例：
✅ 正确：
  - 读取：需求画场景.md
  - 解析：场景1、场景2、场景3...
  - 提取：每个场景的描述和验收准则

❌ 错误：
  - 从其他文件读取场景信息
  - 手动输入场景信息
  - 遗漏部分场景


## [规则 2] MFQ-M 格式标准（优先级：关键）
# 严格按照 MFQ-M 格式生成单功能测试用例

说明：
- **M**odel（模型）：测试对象/被测功能模块
- **F**unction（功能）：具体功能点
- **Q**uality（质量）：质量属性/测试维度
- **M**ethod（方法）：测试方法/测试步骤

MFQ-M 标准格式：
```markdown
### MFQ-M-001：[功能名称]-[质量属性]

#### **Model（模型）**
[被测对象/功能模块描述]

#### **Function（功能）**
[具体功能点描述]

#### **Quality（质量）**
[质量属性：功能正确性/性能/安全性/可用性/兼容性等]

#### **Method（方法）**
**前置条件：**
- [条件1]
- [条件2]

**测试步骤：**
1. [步骤1]
2. [步骤2]
3. [步骤3]

**预期结果：**
- [结果1]
- [结果2]

**验收准则映射：**
|Given|When|Then|
|-|-|-|
|[映射到Given]|[映射到When]|[映射到Then]|
```

格式要求：
- 每个场景至少生成一个 MFQ-M 测试用例
- 测试用例编号格式：`MFQ-M-XXX`（XXX为三位数字，从001开始）
- 标题格式：`[功能名称]-[质量属性]`
- 必须包含四个核心部分：Model、Function、Quality、Method
- Method部分必须映射到原始场景的验收准则（Given|When|Then）

后果：
- 格式不一致会导致测试用例无法被测试管理系统识别
- 缺少核心部分会导致测试用例不完整


## [规则 3] TCO 优化原则（优先级：高）
# 遵循测试用例优化原则，减少冗余，提高效率

说明：
- **T**est **C**ase **O**ptimization：测试用例优化
- 避免重复测试用例
- 合并相似场景
- 优先覆盖关键路径
- 平衡测试覆盖率和执行成本

TCO 优化策略：

1. **场景合并规则**
   - 如果多个场景的 Given|When|Then 高度相似，考虑合并为一个测试用例
   - 合并时保留所有关键测试点，使用参数化测试
   - 示例：场景1和场景2都是登录场景，只是用户名不同 → 合并为一个参数化测试用例

2. **优先级规则**
   - P0（关键）：核心功能、主流程、安全相关
   - P1（重要）：常用功能、次要流程
   - P2（一般）：边界条件、异常场景
   - 优先生成 P0 和 P1 级别的测试用例

3. **去重规则**
   - 检查是否已存在相同或高度相似的测试用例
   - 如果存在，更新现有用例而非创建新用例
   - 使用场景描述和验收准则作为去重依据

4. **覆盖度规则**
   - 确保每个场景至少有一个对应的测试用例
   - 关键场景（P0）应生成多个测试用例覆盖不同质量属性
   - 平衡功能测试、性能测试、安全测试等

5. **成本效益规则**
   - 优先生成自动化成本低、执行快速的测试用例
   - 复杂场景可以拆分为多个简单测试用例
   - 避免生成难以维护或执行成本过高的测试用例

后果：
- 未优化会导致测试用例冗余，增加维护成本
- 过度优化可能导致测试覆盖率不足


## [规则 4] 场景到测试用例的映射规则（优先级：高）
# 将需求场景准确映射为测试用例

说明：
- 从场景的**场景描述**中提取功能名称和质量属性
- 从**验收准则**表格中提取测试步骤和预期结果
- 保持测试用例与需求场景的一致性

映射规则：

1. **Model（模型）提取**
   - 从场景描述中识别被测对象/功能模块
   - 示例："正常登录场景" → Model: "用户登录模块"

2. **Function（功能）提取**
   - 从场景描述中提取具体功能点
   - 示例："正常登录场景正常登录流程验证" → Function: "用户登录功能"

3. **Quality（质量）识别**
   - 从场景描述中识别质量属性
   - 默认质量属性：功能正确性
   - 如果场景描述包含性能、安全等关键词，识别对应质量属性
   - 示例："用xxxx支持正常解密" → Quality: "安全性"

4. **Method（方法）映射**
   - **前置条件** ← Given列内容
   - **测试步骤** ← When列内容（拆分为步骤）
   - **预期结果** ← Then列内容
   - **验收准则映射** ← 完整的Given|When|Then表格

示例映射：
```markdown
输入场景：
### 场景1：
#### **场景描述**
正常登录场景正常登录流程验证，用xxxx支持正常解密。

#### **验收准则**
|Given|When|Then|
|-|-|-|
|用户已注册账号且系统正常运行|用户打开登录页面，输入正确的用户名和密码，点击登录按钮|登录成功，跳转到首页，显示用户信息|

输出测试用例：
### MFQ-M-001：用户登录-功能正确性

#### **Model（模型）**
用户登录模块

#### **Function（功能）**
用户登录功能

#### **Quality（质量）**
功能正确性

#### **Method（方法）**
**前置条件：**
- 用户已注册账号
- 系统正常运行

**测试步骤：**
1. 打开登录页面
2. 输入正确的用户名和密码
3. 点击登录按钮

**预期结果：**
- 登录成功
- 跳转到首页
- 显示用户信息

**验收准则映射：**
|Given|When|Then|
|-|-|-|
|用户已注册账号且系统正常运行|用户打开登录页面，输入正确的用户名和密码，点击登录按钮|登录成功，跳转到首页，显示用户信息|
```


## [规则 5] 测试用例文件生成规则（优先级：中）
# 生成标准化的测试用例文件

说明：
- 输出文件：`MFQ-M-单功能测试用例.md`
- 文件格式：Markdown
- 文件结构：按场景顺序组织测试用例
- 追加模式：如果文件已存在，追加新用例

文件结构：
```markdown
# MFQ-M 单功能测试用例

> 生成时间：2025-01-XX
> 来源：需求画场景.md
> 优化原则：TCO

## 场景1 对应的测试用例

### MFQ-M-001：[功能名称]-[质量属性]
[测试用例内容]

### MFQ-M-002：[功能名称]-[质量属性]（如果需要多个用例）
[测试用例内容]

---

## 场景2 对应的测试用例

### MFQ-M-003：[功能名称]-[质量属性]
[测试用例内容]

---
```

文件命名规则：
- 主文件：`MFQ-M-单功能测试用例.md`
- 如果场景数量较多，可以按模块拆分文件
- 文件命名：`MFQ-M-[模块名称]-测试用例.md`

追加规则：
- 如果文件已存在，检查是否已有相同场景的测试用例
- 如果已有，更新现有用例（基于场景编号匹配）
- 如果没有，追加新用例
- 使用 `---` 分隔不同场景的测试用例


## [规则 6] 质量属性识别规则（优先级：中）
# 准确识别测试用例的质量属性

说明：
- 从场景描述中识别质量属性关键词
- 为每个测试用例分配合适的质量属性
- 一个场景可以生成多个不同质量属性的测试用例

质量属性分类：

1. **功能正确性**（默认）
   - 关键词：正常、流程、验证、功能
   - 示例：正常登录场景 → 功能正确性

2. **性能**
   - 关键词：性能、速度、响应时间、并发、负载
   - 示例：高并发登录场景 → 性能

3. **安全性**
   - 关键词：安全、加密、解密、权限、认证、授权
   - 示例：用xxxx支持正常解密 → 安全性

4. **可用性**
   - 关键词：易用、界面、交互、用户体验
   - 示例：界面友好性验证 → 可用性

5. **兼容性**
   - 关键词：兼容、浏览器、设备、版本
   - 示例：多浏览器兼容性 → 兼容性

6. **可靠性**
   - 关键词：稳定、异常、错误处理、容错
   - 示例：异常场景处理 → 可靠性

识别规则：
- 如果场景描述包含多个质量属性关键词，生成多个测试用例
- 如果场景描述不包含明确的质量属性，默认为"功能正确性"
- 关键场景（P0）应覆盖多个质量属性


## [规则 7] 测试用例编号管理（优先级：中）
# 统一管理测试用例编号

说明：
- 测试用例编号格式：`MFQ-M-XXX`（XXX为三位数字）
- 编号从001开始，依次递增
- 编号全局唯一，不重复
- 如果文件已存在，继续使用最大编号+1

编号规则：
1. 读取现有文件，找到最大编号
2. 新用例编号 = 最大编号 + 1
3. 如果文件不存在，从001开始
4. 编号连续，不允许跳号（除非明确删除）

示例：
- 第一个用例：MFQ-M-001
- 第二个用例：MFQ-M-002
- 第三个用例：MFQ-M-003
- ...

编号与场景的对应关系：
- 一个场景可以对应多个测试用例（不同质量属性）
- 多个场景可以合并为一个测试用例（TCO优化）
- 编号不强制与场景编号一一对应


## [规则 8] 验证和完整性检查（优先级：高）
# 生成后验证测试用例的完整性

说明：
- 生成测试用例后，必须进行完整性验证
- 确保所有场景都有对应的测试用例
- 确保测试用例格式正确
- 确保验收准则映射准确

验证项：
- [ ] 是否所有场景都生成了至少一个测试用例
- [ ] 每个测试用例是否包含四个核心部分（Model、Function、Quality、Method）
- [ ] Method部分是否包含前置条件、测试步骤、预期结果
- [ ] 验收准则映射是否与原始场景一致
- [ ] 测试用例编号是否连续且唯一
- [ ] 文件格式是否正确（Markdown语法）
- [ ] TCO优化是否合理（无过度冗余）

验证失败处理：
- 如果验证失败，输出详细的错误信息
- 列出缺失或错误的测试用例
- 提供修复建议


## [规则 9] 输出反馈和统计（优先级：低）
# 提供清晰的生成结果反馈

说明：
- 生成完成后，提供统计信息
- 告知用户生成的测试用例数量和文件位置
- 提供优化建议

反馈格式：
- 成功：`成功生成 [N] 个MFQ-M测试用例，已保存到 MFQ-M-单功能测试用例.md`
- 统计：`场景数量：[X]，测试用例数量：[Y]，优化合并：[Z]个`
- 警告：`检测到 [问题描述]，已处理：[处理方式]`
- 错误：`生成失败：[具体错误原因]`

统计信息：
- 输入场景数量
- 生成测试用例数量
- TCO优化合并数量
- 各质量属性分布（功能正确性、性能、安全性等）
- P0/P1/P2优先级分布


# ============================================
# 摘要 - 关键规则（最高优先级）
# ============================================

1. ✅ [规则 1] 从需求画场景.md读取场景（关键）
2. ✅ [规则 2] MFQ-M 格式标准（关键）
3. ✅ [规则 3] TCO 优化原则（高）
4. ✅ [规则 4] 场景到测试用例的映射规则（高）
5. ✅ [规则 5] 测试用例文件生成规则（中）
6. ✅ [规则 6] 质量属性识别规则（中）
7. ✅ [规则 7] 测试用例编号管理（中）
8. ✅ [规则 8] 验证和完整性检查（高）
9. ✅ [规则 9] 输出反馈和统计（低）


# ============================================
# MFQ-M 格式模板
# ============================================

标准模板：
```markdown
### MFQ-M-XXX：[功能名称]-[质量属性]

#### **Model（模型）**
[被测对象/功能模块描述]

#### **Function（功能）**
[具体功能点描述]

#### **Quality（质量）**
[质量属性：功能正确性/性能/安全性/可用性/兼容性/可靠性]

#### **Method（方法）**
**前置条件：**
- [条件1]
- [条件2]

**测试步骤：**
1. [步骤1]
2. [步骤2]
3. [步骤3]

**预期结果：**
- [结果1]
- [结果2]

**验收准则映射：**
|Given|When|Then|
|-|-|-|
|[Given内容]|[When内容]|[Then内容]|
```


# ============================================
# 使用示例
# ============================================

示例 1：从需求画场景.md生成测试用例
```
用户输入：
"请根据需求画场景.md生成MFQ-M单功能测试用例"

AI 处理：
1. 读取 需求画场景.md 文件
2. 解析所有场景（场景1、场景2...）
3. 对每个场景：
   - 提取Model、Function、Quality
   - 映射Method（前置条件、测试步骤、预期结果）
   - 应用TCO优化（去重、合并、优先级）
4. 生成测试用例文件：MFQ-M-单功能测试用例.md
5. 验证完整性
6. 输出："成功生成 5 个MFQ-M测试用例，已保存到 MFQ-M-单功能测试用例.md"
```

示例 2：TCO优化合并场景
```
输入场景：
场景1：正常登录（用户名：user1）
场景2：正常登录（用户名：user2）

AI 处理：
1. 识别两个场景高度相似
2. 应用TCO优化规则
3. 合并为一个参数化测试用例：
   MFQ-M-001：用户登录-功能正确性（参数化：用户名）
4. 在测试步骤中使用参数：[用户名]
```

示例 3：多质量属性场景
```
输入场景：
场景1：正常登录场景正常登录流程验证，用xxxx支持正常解密。

AI 处理：
1. 识别质量属性：功能正确性、安全性
2. 生成两个测试用例：
   - MFQ-M-001：用户登录-功能正确性
   - MFQ-M-002：用户登录-安全性
3. 两个用例共享Model和Function，但Quality和Method不同
```


# ============================================
# TCO 优化示例
# ============================================

优化前（冗余）：
- MFQ-M-001：用户登录-功能正确性（用户名：user1）
- MFQ-M-002：用户登录-功能正确性（用户名：user2）
- MFQ-M-003：用户登录-功能正确性（用户名：user3）

优化后（合并）：
- MFQ-M-001：用户登录-功能正确性（参数化：用户名）
  - 测试数据：[user1, user2, user3]

优化效果：
- 测试用例数量：3 → 1（减少67%）
- 维护成本：降低
- 测试覆盖率：保持不变


# ============================================
# 版本历史
# ============================================
# v1.0 (2025-01-XX) - 初始版本，定义MFQ-M单功能测试用例生成规范，符合TCO原则
# ============================================
