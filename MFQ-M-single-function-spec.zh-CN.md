---
trigger: manual
---

# MFQ-M 单功能生成规范 v1.1
# ============================================
# 基于需求场景生成单功能测试用例的规则和要求
# 严格遵循 TCO（Testing Coverage Outline）测试覆盖要点
#
# 使用方法：
# 1. 将此文件放在项目根目录
# 2. 在 AI 对话中使用 @MFQ-M-single-function-spec.zh-CN.md 引用
# 3. AI 将自动遵循这些规则生成单功能测试用例
#
# 输入来源：需求画场景.md
# 输出格式：MFQ-M 单功能测试用例
# 分析方法：TCO（Testing Coverage Outline）测试覆盖要点
# 参考：https://www.cnblogs.com/josie-xu/p/10536592.html
# 最后更新：2025-01-XX
# ============================================

## [规则 1] 从需求画场景.md读取场景（优先级：关键）
# 必须从需求画场景.md文件中读取场景信息

说明：
- 读取当前目录下的 `需求画场景.md` 文件
- 解析文件中的所有场景（格式：`### 场景N：`）
- 提取每个场景的**场景描述**和**验收准则**（Given|When|Then表格）
- 如果文件不存在，提示用户先执行需求提取流程

工作流程：
1. 检查 `需求画场景.md` 文件是否存在
2. 如果不存在，提示："需求画场景.md 文件不存在，请先执行需求提取流程"
3. 如果存在，解析文件中的所有场景
4. 提取每个场景的完整信息（场景编号、描述、验收准则）

后果：
- 未从正确来源读取会导致测试用例与需求不匹配
- 遗漏场景会导致测试覆盖率不足

示例：
✅ 正确：
  - 读取：需求画场景.md
  - 解析：场景1、场景2、场景3...
  - 提取：每个场景的描述和验收准则

❌ 错误：
  - 从其他文件读取场景信息
  - 手动输入场景信息
  - 遗漏部分场景


## [规则 2] MFQ-M 格式标准（优先级：关键）
# 严格按照 MFQ-M 格式生成单功能测试用例

说明：
- **M**odel（模型）：测试对象/被测功能模块
- **F**unction（功能）：具体功能点
- **Q**uality（质量）：质量属性/测试维度
- **M**ethod（方法）：测试方法/测试步骤

MFQ-M 标准格式：
```markdown
### MFQ-M-001：[功能名称]-[质量属性]

#### **Model（模型）**
[被测对象/功能模块描述]

#### **Function（功能）**
[具体功能点描述]

#### **Quality（质量）**
[质量属性：功能正确性/性能/安全性/可用性/兼容性等]

#### **Method（方法）**
**前置条件：**
- [条件1]
- [条件2]

**测试步骤：**
1. [步骤1]
2. [步骤2]
3. [步骤3]

**预期结果：**
- [结果1]
- [结果2]

**验收准则映射：**
|Given|When|Then|
|-|-|-|
|[映射到Given]|[映射到When]|[映射到Then]|
```

格式要求：
- 每个场景至少生成一个 MFQ-M 测试用例
- 测试用例编号格式：`MFQ-M-XXX`（XXX为三位数字，从001开始）
- 标题格式：`[功能名称]-[质量属性]`
- 必须包含四个核心部分：Model、Function、Quality、Method
- Method部分必须映射到原始场景的验收准则（Given|When|Then）

后果：
- 格式不一致会导致测试用例无法被测试管理系统识别
- 缺少核心部分会导致测试用例不完整


## [规则 3] TCO 测试覆盖要点（优先级：高）
# 遵循 TCO（Testing Coverage Outline）方法，整合信息并分层提炼

说明：
- **T**esting **C**overage **O**utline：测试覆盖要点
- TCO 是把从需求场景中获取到的测试信息进行整合，对被测对象进行分层提炼
- 找出单功能 M（Model）、功能交互 F（Function）、质量属性 Q（Quality）
- 同时识别出风险 Risk，列出疑问 Issue
- 在分析过程中也可以提前把一些变量参数单独列入 Data

TCO 分层提炼策略：

1. **单功能 M（Model）识别**
   - 从场景描述中识别被测对象/功能模块
   - 对被测对象进行分层，识别核心功能模块
   - 示例："正常登录场景" → Model: "用户登录模块"

2. **功能交互 F（Function）识别**
   - 识别功能点之间的交互关系
   - 识别功能组合和依赖关系
   - 示例："登录后查看个人信息" → Function: "登录功能与个人信息查看功能的交互"

3. **质量属性 Q（Quality）识别**
   - 识别需要验证的质量属性维度
   - 包括：功能正确性、性能、安全性、可用性、兼容性、可靠性等
   - 示例："用xxxx支持正常解密" → Quality: "安全性"

4. **风险 Risk 识别**
   - 识别测试中的潜在风险点
   - 标记高风险场景和关键路径
   - 优先覆盖高风险场景

5. **疑问 Issue 记录**
   - 记录测试分析过程中的疑问
   - 需要澄清的需求点
   - 不确定的测试场景

6. **数据 Data 准备**
   - 识别测试中需要的变量参数
   - 准备测试数据和测试环境配置
   - 参数化测试用例

TCO 输出结构：
```markdown
## TCO 测试覆盖要点

### M（单功能）
- [功能模块1]
- [功能模块2]

### F（功能交互）
- [功能交互1]
- [功能交互2]

### Q（质量属性）
- [质量属性1]
- [质量属性2]

### Risk（风险）
- [风险点1]
- [风险点2]

### Issue（疑问）
- [疑问1]
- [疑问2]

### Data（数据）
- [测试数据1]
- [测试数据2]
```

后果：
- 未进行TCO分析会导致测试覆盖不全面
- 遗漏风险识别可能导致关键缺陷未被发现


## [规则 4] 场景到测试用例的映射规则（优先级：高）
# 将需求场景准确映射为测试用例

说明：
- 从场景的**场景描述**中提取功能名称和质量属性
- 从**验收准则**表格中提取测试步骤和预期结果
- 保持测试用例与需求场景的一致性

映射规则：

1. **Model（模型）提取**
   - 从场景描述中识别被测对象/功能模块
   - 示例："正常登录场景" → Model: "用户登录模块"

2. **Function（功能）提取**
   - 从场景描述中提取具体功能点
   - 示例："正常登录场景正常登录流程验证" → Function: "用户登录功能"

3. **Quality（质量）识别**
   - 从场景描述中识别质量属性
   - 默认质量属性：功能正确性
   - 如果场景描述包含性能、安全等关键词，识别对应质量属性
   - 示例："用xxxx支持正常解密" → Quality: "安全性"

4. **Method（方法）映射**
   - **前置条件** ← Given列内容
   - **测试步骤** ← When列内容（拆分为步骤）
   - **预期结果** ← Then列内容
   - **验收准则映射** ← 完整的Given|When|Then表格

示例映射：
```markdown
输入场景：
### 场景1：
#### **场景描述**
正常登录场景正常登录流程验证，用xxxx支持正常解密。

#### **验收准则**
|Given|When|Then|
|-|-|-|
|用户已注册账号且系统正常运行|用户打开登录页面，输入正确的用户名和密码，点击登录按钮|登录成功，跳转到首页，显示用户信息|

输出测试用例：
### MFQ-M-001：用户登录-功能正确性

#### **Model（模型）**
用户登录模块

#### **Function（功能）**
用户登录功能

#### **Quality（质量）**
功能正确性

#### **Method（方法）**
**前置条件：**
- 用户已注册账号
- 系统正常运行

**测试步骤：**
1. 打开登录页面
2. 输入正确的用户名和密码
3. 点击登录按钮

**预期结果：**
- 登录成功
- 跳转到首页
- 显示用户信息

**验收准则映射：**
|Given|When|Then|
|-|-|-|
|用户已注册账号且系统正常运行|用户打开登录页面，输入正确的用户名和密码，点击登录按钮|登录成功，跳转到首页，显示用户信息|
```


## [规则 5] 测试用例文件生成规则（优先级：中）
# 生成标准化的测试用例文件

说明：
- 输出文件：`MFQ-M-单功能测试用例.md`
- 文件格式：Markdown
- 文件结构：按场景顺序组织测试用例
- 追加模式：如果文件已存在，追加新用例

文件结构：
```markdown
# MFQ-M 单功能测试用例

> 生成时间：2025-01-XX
> 来源：需求画场景.md
> 分析方法：TCO（Testing Coverage Outline）

## 场景1 对应的测试用例

### MFQ-M-001：[功能名称]-[质量属性]
[测试用例内容]

### MFQ-M-002：[功能名称]-[质量属性]（如果需要多个用例）
[测试用例内容]

---

## 场景2 对应的测试用例

### MFQ-M-003：[功能名称]-[质量属性]
[测试用例内容]

---
```

文件命名规则：
- 主文件：`MFQ-M-单功能测试用例.md`
- 如果场景数量较多，可以按模块拆分文件
- 文件命名：`MFQ-M-[模块名称]-测试用例.md`

追加规则：
- 如果文件已存在，检查是否已有相同场景的测试用例
- 如果已有，更新现有用例（基于场景编号匹配）
- 如果没有，追加新用例
- 使用 `---` 分隔不同场景的测试用例


## [规则 6] 质量属性识别规则（优先级：中）
# 准确识别测试用例的质量属性

说明：
- 从场景描述中识别质量属性关键词
- 为每个测试用例分配合适的质量属性
- 一个场景可以生成多个不同质量属性的测试用例

质量属性分类：

1. **功能正确性**（默认）
   - 关键词：正常、流程、验证、功能
   - 示例：正常登录场景 → 功能正确性

2. **性能**
   - 关键词：性能、速度、响应时间、并发、负载
   - 示例：高并发登录场景 → 性能

3. **安全性**
   - 关键词：安全、加密、解密、权限、认证、授权
   - 示例：用xxxx支持正常解密 → 安全性

4. **可用性**
   - 关键词：易用、界面、交互、用户体验
   - 示例：界面友好性验证 → 可用性

5. **兼容性**
   - 关键词：兼容、浏览器、设备、版本
   - 示例：多浏览器兼容性 → 兼容性

6. **可靠性**
   - 关键词：稳定、异常、错误处理、容错
   - 示例：异常场景处理 → 可靠性

识别规则：
- 如果场景描述包含多个质量属性关键词，生成多个测试用例
- 如果场景描述不包含明确的质量属性，默认为"功能正确性"
- 关键场景（P0）应覆盖多个质量属性


## [规则 7] 测试用例编号管理（优先级：中）
# 统一管理测试用例编号

说明：
- 测试用例编号格式：`MFQ-M-XXX`（XXX为三位数字）
- 编号从001开始，依次递增
- 编号全局唯一，不重复
- 如果文件已存在，继续使用最大编号+1

编号规则：
1. 读取现有文件，找到最大编号
2. 新用例编号 = 最大编号 + 1
3. 如果文件不存在，从001开始
4. 编号连续，不允许跳号（除非明确删除）

示例：
- 第一个用例：MFQ-M-001
- 第二个用例：MFQ-M-002
- 第三个用例：MFQ-M-003
- ...

编号与场景的对应关系：
- 一个场景可以对应多个测试用例（不同质量属性）
- 通过TCO分析，可以识别出场景中的M、F、Q，从而生成对应的测试用例
- 编号不强制与场景编号一一对应


## [规则 8] 验证和完整性检查（优先级：高）
# 生成后验证测试用例的完整性

说明：
- 生成测试用例后，必须进行完整性验证
- 确保所有场景都有对应的测试用例
- 确保测试用例格式正确
- 确保验收准则映射准确

验证项：
- [ ] 是否所有场景都生成了至少一个测试用例
- [ ] 每个测试用例是否包含四个核心部分（Model、Function、Quality、Method）
- [ ] Method部分是否包含前置条件、测试步骤、预期结果
- [ ] 验收准则映射是否与原始场景一致
- [ ] 测试用例编号是否连续且唯一
- [ ] 文件格式是否正确（Markdown语法）
- [ ] TCO分析是否完整（M、F、Q、Risk、Issue、Data是否都识别）

验证失败处理：
- 如果验证失败，输出详细的错误信息
- 列出缺失或错误的测试用例
- 提供修复建议


## [规则 9] 输出反馈和统计（优先级：低）
# 提供清晰的生成结果反馈

说明：
- 生成完成后，提供统计信息
- 告知用户生成的测试用例数量和文件位置
- 提供TCO分析结果摘要

反馈格式：
- 成功：`成功生成 [N] 个MFQ-M测试用例，已保存到 MFQ-M-单功能测试用例.md`
- 统计：`场景数量：[X]，测试用例数量：[Y]，TCO分析识别：[M个单功能，F个功能交互，Q个质量属性]`
- 警告：`检测到 [问题描述]，已处理：[处理方式]`
- 错误：`生成失败：[具体错误原因]`

统计信息：
- 输入场景数量
- 生成测试用例数量
- TCO分析结果：
  - M（单功能）数量
  - F（功能交互）数量
  - Q（质量属性）数量
  - Risk（风险点）数量
  - Issue（疑问）数量
- 各质量属性分布（功能正确性、性能、安全性等）


# ============================================
# 摘要 - 关键规则（最高优先级）
# ============================================

1. ✅ [规则 1] 从需求画场景.md读取场景（关键）
2. ✅ [规则 2] MFQ-M 格式标准（关键）
3. ✅ [规则 3] TCO 测试覆盖要点（高）
4. ✅ [规则 4] 场景到测试用例的映射规则（高）
5. ✅ [规则 5] 测试用例文件生成规则（中）
6. ✅ [规则 6] 质量属性识别规则（中）
7. ✅ [规则 7] 测试用例编号管理（中）
8. ✅ [规则 8] 验证和完整性检查（高）
9. ✅ [规则 9] 输出反馈和统计（低）


# ============================================
# MFQ-M 格式模板
# ============================================

标准模板：
```markdown
### MFQ-M-XXX：[功能名称]-[质量属性]

#### **Model（模型）**
[被测对象/功能模块描述]

#### **Function（功能）**
[具体功能点描述]

#### **Quality（质量）**
[质量属性：功能正确性/性能/安全性/可用性/兼容性/可靠性]

#### **Method（方法）**
**前置条件：**
- [条件1]
- [条件2]

**测试步骤：**
1. [步骤1]
2. [步骤2]
3. [步骤3]

**预期结果：**
- [结果1]
- [结果2]

**验收准则映射：**
|Given|When|Then|
|-|-|-|
|[Given内容]|[When内容]|[Then内容]|
```


# ============================================
# 使用示例
# ============================================

示例 1：从需求画场景.md生成测试用例
```
用户输入：
"请根据需求画场景.md生成MFQ-M单功能测试用例"

AI 处理：
1. 读取 需求画场景.md 文件
2. 解析所有场景（场景1、场景2...）
3. 应用TCO方法进行分层提炼：
   - 识别M（单功能模块）
   - 识别F（功能交互）
   - 识别Q（质量属性）
   - 识别Risk（风险点）
   - 记录Issue（疑问）
   - 准备Data（测试数据）
4. 对每个场景：
   - 提取Model、Function、Quality
   - 映射Method（前置条件、测试步骤、预期结果）
5. 生成测试用例文件：MFQ-M-单功能测试用例.md
6. 验证完整性
7. 输出："成功生成 5 个MFQ-M测试用例，已保存到 MFQ-M-单功能测试用例.md"
```

示例 2：TCO分层提炼场景
```
输入场景：
场景1：正常登录场景正常登录流程验证，用xxxx支持正常解密。

AI 处理：
1. 应用TCO方法分析：
   - M（单功能）：用户登录模块
   - F（功能交互）：登录功能与解密功能的交互
   - Q（质量属性）：功能正确性、安全性
   - Risk（风险）：解密失败可能导致登录失败
   - Issue（疑问）：xxxx具体指什么加密方式？
   - Data（数据）：用户名、密码、加密参数
2. 生成测试用例：
   - MFQ-M-001：用户登录-功能正确性
   - MFQ-M-002：用户登录-安全性（解密验证）
```

示例 3：多质量属性场景
```
输入场景：
场景1：正常登录场景正常登录流程验证，用xxxx支持正常解密。

AI 处理：
1. 识别质量属性：功能正确性、安全性
2. 生成两个测试用例：
   - MFQ-M-001：用户登录-功能正确性
   - MFQ-M-002：用户登录-安全性
3. 两个用例共享Model和Function，但Quality和Method不同
```


# ============================================
# TCO 测试覆盖要点示例
# ============================================

场景：正常登录场景正常登录流程验证，用xxxx支持正常解密。

TCO 分析结果：

### M（单功能）
- 用户登录模块
- 密码解密模块

### F（功能交互）
- 登录功能与解密功能的交互
- 登录功能与用户认证功能的交互

### Q（质量属性）
- 功能正确性：登录流程是否正常
- 安全性：密码解密是否正确

### Risk（风险）
- 高风险：解密失败导致登录失败
- 中风险：密码传输过程中的安全性

### Issue（疑问）
- xxxx具体指什么加密方式？
- 解密失败时的错误处理机制是什么？

### Data（数据）
- 用户名：test_user
- 密码：encrypted_password
- 加密参数：xxxx

基于TCO分析生成的测试用例：
- MFQ-M-001：用户登录-功能正确性
- MFQ-M-002：用户登录-安全性（解密验证）


# ============================================
# 版本历史
# ============================================
# v1.1 (2025-01-XX) - 纠正TCO定义，更新为Testing Coverage Outline（测试覆盖要点）
# v1.0 (2025-01-XX) - 初始版本，定义MFQ-M单功能测试用例生成规范
# ============================================
