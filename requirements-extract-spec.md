---
trigger: manual
---

# 需求提取规范 v1.2
# ============================================
# AI 辅助测试的需求分析规则和要求
# 需求分析时严格遵循这些规则
#
# 使用方法：
# 1. 将此文件放在项目根目录
# 2. 在 AI 对话中使用 @requirements-extract-spec.md 引用
# 3. AI 将自动遵循这些规则进行需求提取
#
# 支持的页面类型：
# - 需求实例化页面：提取"画场景"章节 → 需求画场景.md
# - 需求方案页面：提取"性能设计"和"质量属性设计"章节 → 需求方案-Q.md
#
# 最后更新：2025-01-XX
# ============================================

## [规则 1] 使用本地 MCP 服务提取页面信息（优先级：关键）
# 必须使用本地 icenter-markdown MCP 服务完成页面信息的提取

说明：
- 从用户输入的页面链接中，调用本地 MCP 完成信息提取
- 支持两种页面类型：
  - **需求实例化**页面：提取"画场景"章节
  - **需求方案**页面：提取"性能设计"和"质量属性设计"章节
- 必须使用本地 icenter-markdown MCP 服务完成页面信息的提取
- 如果服务不可用，直接提示用户进行确认，不要随意切换其他方式
- 不允许使用其他替代方案（如直接 HTTP 请求、第三方爬虫等）

后果：
- 使用非 MCP 方式可能导致信息提取不准确或格式不一致
- 绕过 MCP 服务可能违反系统架构要求

工作流程：
1. 接收用户提供的页面链接（可以是需求实例化或需求方案页面）
2. 检查本地 icenter-markdown MCP 服务是否可用
3. 如果可用，调用 MCP 服务提取页面信息
4. 识别页面类型（需求实例化 或 需求方案）
5. 根据页面类型执行对应的提取规则
6. 如果不可用，提示用户："icenter-markdown MCP 服务当前不可用，请确认服务状态"

示例：
✅ 正确：
  - 用户提供链接：https://example.com/requirement-instance（需求实例化）
  - 调用：icenter-markdown MCP 服务提取页面内容
  - 识别：需求实例化页面
  - 提取："画场景"章节
  
  - 用户提供链接：https://example.com/requirement-solution（需求方案）
  - 调用：icenter-markdown MCP 服务提取页面内容
  - 识别：需求方案页面
  - 提取："性能设计"和"质量属性设计"章节

❌ 错误：
  - 用户提供链接：https://example.com/requirement-page
  - 直接使用 fetch/axios 请求页面
  - 使用第三方 HTML 解析库提取内容


## [规则 2] 需求实例化页面：提取"画场景"章节（优先级：关键）
# 如果页面包含"需求实例化"字样，必须重点提取"画场景"章节

说明：
- 如果用户提供的页面链接包含**需求实例化**字样，必须输出并告知用户该页面为**需求实例化**页面
- 如果是**需求实例化**页面，则需要重点提取**画场景**章节的所有信息，不允许遗漏
- **画场景**章节会包含多个场景内容，需要按照格式提取并写入到当前目录的**需求画场景.md**中
- 提取的场景信息应保持原始格式和结构
- **需求画场景.md** 用于生成 MFQ-M（单功能）和 MFQ-F（功能交互）测试用例

后果：
- 遗漏"画场景"章节会导致需求分析不完整
- 格式不一致会影响后续的需求理解和测试用例生成

工作流程：
1. 检查页面内容是否包含"需求实例化"关键字
2. 如果包含，告知用户："检测到需求实例化页面，将重点提取'画场景'章节"
3. 定位并提取"画场景"章节的所有内容
4. 验证是否提取完整（检查场景数量、格式等）
5. 将提取的内容写入到 `需求画场景.md` 文件
6. 如果文件已存在，追加内容并保持风格一致

提取格式要求：
- 保持原始章节标题层级（H3 为场景标题，H4 为场景描述和验收准则）
- 保留场景的编号和标题（格式：`### 场景1：`）
- 必须包含 **场景描述** 子章节（格式：`#### **场景描述**`）
- 必须包含 **验收准则** 子章节（格式：`#### **验收准则**`）
- 验收准则必须保留表格格式，包含 Given|When|Then 三列
- 保留表格中的所有内容，不允许遗漏任何单元格
- 保持 Markdown 表格格式的完整性

场景标准格式：
```markdown
### 场景1：
#### **场景描述**
正常登录场景正常登录流程验证，用xxxx支持正常解密。

#### **验收准则**
|Given|When|Then|
|-|-|-|
|xxxxx|xxxxx|xxxxx|
```

示例：
✅ 正确：
  页面内容检测：
  - 发现"需求实例化"关键字
  - 输出："检测到需求实例化页面，开始提取'画场景'章节"
  
  提取结果：
  ```markdown
  # 需求画场景.md
  
  ### 场景1：
  #### **场景描述**
  正常登录场景正常登录流程验证，用xxxx支持正常解密。
  
  #### **验收准则**
  |Given|When|Then|
  |-|-|-|
  |用户已注册账号且系统正常运行|用户打开登录页面，输入正确的用户名和密码，点击登录按钮|登录成功，跳转到首页，显示用户信息|
  
  ### 场景2：
  #### **场景描述**
  用户注册场景，验证新用户注册流程。
  
  #### **验收准则**
  |Given|When|Then|
  |-|-|-|
  |用户未注册账号|用户打开注册页面，填写注册信息，点击注册按钮|注册成功，跳转到登录页面|
  ```

❌ 错误：
  - 未检测"需求实例化"关键字
  - 提取了其他章节但遗漏了"画场景"
  - 提取的场景信息格式混乱
  - 未写入到指定文件


## [规则 3] 需求方案页面：提取"性能设计"和"质量属性设计"章节（优先级：关键）
# 如果页面包含"需求方案"字样，必须重点提取"性能设计"和"质量属性设计"章节

说明：
- 如果用户提供的页面链接包含**需求方案**字样，必须输出并告知用户该页面为**需求方案**页面
- 如果是**需求方案**页面，则需要重点提取以下章节的所有信息，不允许遗漏：
  - **性能设计**章节
  - **质量属性设计**章节
- 提取的内容需要按照格式写入到当前目录的**需求方案-Q.md**中
- 提取的信息应保持原始格式和结构
- **需求方案-Q.md** 用于生成 MFQ-Q（质量属性）测试用例

工作流程：
1. 检查页面内容是否包含"需求方案"关键字
2. 如果包含，告知用户："检测到需求方案页面，将重点提取'性能设计'和'质量属性设计'章节"
3. 定位并提取"性能设计"章节的所有内容
4. 定位并提取"质量属性设计"章节的所有内容
5. 验证是否提取完整（检查章节完整性、格式等）
6. 将提取的内容写入到 `需求方案-Q.md` 文件
7. 如果文件已存在，追加内容并保持风格一致
8. **同时检查是否存在"组件接口说明"章节**（见规则 3.1）
9. **同时检查是否存在"UX设计"章节**（见规则 3.2）

提取格式要求：
- 保持原始章节标题层级
- 保留所有子章节和内容
- 保留表格、列表、代码块等格式
- 保持 Markdown 格式的完整性

示例：
✅ 正确：
  页面内容检测：
  - 发现"需求方案"关键字
  - 输出："检测到需求方案页面，开始提取'性能设计'和'质量属性设计'章节"
  
  提取结果：
  ```markdown
  # 需求方案-Q.md
  
  ## 性能设计
  [性能设计章节的完整内容]
  
  ## 质量属性设计
  [质量属性设计章节的完整内容]
  ```

❌ 错误：
  - 未检测"需求方案"关键字
  - 提取了其他章节但遗漏了"性能设计"或"质量属性设计"
  - 提取的信息格式混乱
  - 未写入到指定文件


## [规则 3.1] 需求方案页面：提取"组件接口说明"章节（优先级：关键）
# 如果需求方案页面包含"组件接口说明"章节，必须提取并生成接口规格.md

说明：
- 在处理**需求方案**页面时，必须检查是否存在**组件接口说明**章节（包含其所有子章节）
- 如果存在"组件接口说明"章节，则需要判断是否涉及组件接口内容
- 如果涉及组件接口内容，必须提取该章节及其所有子章节的所有信息，不允许遗漏
- 提取的内容需要按照格式写入到当前目录的**接口规格.md**中
- 提取的信息应保持原始格式和结构
- **接口规格.md** 用于接口设计和开发参考

判断标准：
- 如果"组件接口说明"章节存在且包含以下任一内容，则判定为涉及组件接口：
  - 接口定义（API、方法、函数等）
  - 接口参数说明
  - 接口返回值说明
  - 接口调用示例
  - 接口规范文档
  - 其他接口相关内容

工作流程：
1. 在处理需求方案页面时，检查是否存在"组件接口说明"章节
2. 如果存在，分析章节内容是否涉及组件接口
3. 如果涉及组件接口内容，告知用户："检测到组件接口说明章节，将提取接口规格信息"
4. 定位并提取"组件接口说明"章节的所有内容（包括所有子章节）
5. 验证是否提取完整（检查章节完整性、格式等）
6. 将提取的内容写入到 `接口规格.md` 文件
7. 如果文件已存在，追加内容并保持风格一致
8. 如果不存在或内容为空，不生成文件

提取格式要求：
- 保持原始章节标题层级
- 保留所有子章节和内容
- 保留表格、列表、代码块等格式
- 保留接口定义、参数说明、返回值说明等结构化信息
- 保持 Markdown 格式的完整性

示例：
✅ 正确：
  页面内容检测：
  - 发现"需求方案"关键字
  - 发现"组件接口说明"章节
  - 分析章节内容，确认涉及组件接口
  - 输出："检测到需求方案页面，开始提取'性能设计'、'质量属性设计'和'组件接口说明'章节"
  
  提取结果：
  ```markdown
  # 接口规格.md
  
  ## 组件接口说明
  [组件接口说明章节的完整内容，包括所有子章节]
  
  ### 子章节1
  [子章节内容]
  
  ### 子章节2
  [子章节内容]
  ```

❌ 错误：
  - 未检测"组件接口说明"章节
  - 检测到章节但未判断是否涉及接口内容
  - 提取了章节但遗漏了子章节
  - 提取的信息格式混乱
  - 未写入到指定文件


## [规则 3.2] 需求方案页面：提取"UX设计"章节（优先级：关键）
# 如果需求方案页面包含"UX设计"章节，必须提取并生成WEB UI规格.md

说明：
- 在处理**需求方案**页面时，必须检查是否存在**UX设计**章节（包含其所有子章节）
- 如果存在"UX设计"章节，则需要判断是否涉及WEB页面功能内容
- 如果涉及WEB页面功能内容，必须提取该章节及其所有子章节的所有信息，不允许遗漏
- 提取的内容需要按照格式写入到当前目录的**WEB UI规格.md**中
- 提取的信息应保持原始格式和结构
- **WEB UI规格.md** 用于WEB页面UI设计和开发参考

判断标准：
- 如果"UX设计"章节存在且包含以下任一内容，则判定为涉及WEB页面功能：
  - 页面布局设计
  - 页面元素设计（按钮、表单、列表等）
  - 页面交互设计
  - 页面流程设计
  - 页面原型图或设计稿
  - 页面功能说明
  - WEB界面规范
  - 响应式设计说明
  - 其他WEB页面UI相关内容

工作流程：
1. 在处理需求方案页面时，检查是否存在"UX设计"章节
2. 如果存在，分析章节内容是否涉及WEB页面功能
3. 如果涉及WEB页面功能内容，告知用户："检测到UX设计章节，将提取WEB UI规格信息"
4. 定位并提取"UX设计"章节的所有内容（包括所有子章节）
5. 验证是否提取完整（检查章节完整性、格式等）
6. 将提取的内容写入到 `WEB UI规格.md` 文件
7. 如果文件已存在，追加内容并保持风格一致
8. 如果不存在或内容为空，不生成文件

提取格式要求：
- 保持原始章节标题层级
- 保留所有子章节和内容
- 保留表格、列表、代码块、图片链接等格式
- 保留页面设计、交互设计、流程设计等结构化信息
- 保持 Markdown 格式的完整性

示例：
✅ 正确：
  页面内容检测：
  - 发现"需求方案"关键字
  - 发现"UX设计"章节
  - 分析章节内容，确认涉及WEB页面功能
  - 输出："检测到需求方案页面，开始提取'性能设计'、'质量属性设计'、'组件接口说明'和'UX设计'章节"
  
  提取结果：
  ```markdown
  # WEB UI规格.md
  
  ## UX设计
  [UX设计章节的完整内容，包括所有子章节]
  
  ### 页面布局设计
  [页面布局设计子章节内容]
  
  ### 页面交互设计
  [页面交互设计子章节内容]
  
  ### 页面流程设计
  [页面流程设计子章节内容]
  ```

❌ 错误：
  - 未检测"UX设计"章节
  - 检测到章节但未判断是否涉及WEB页面功能
  - 提取了章节但遗漏了子章节
  - 提取的信息格式混乱
  - 未写入到指定文件


## [规则 4] 信息提取完整性验证（优先级：高）
# 确保提取的信息完整且格式正确

说明：
- 提取完成后，必须验证信息的完整性
- 检查是否遗漏关键章节或内容
- 验证 Markdown 格式是否正确
- 确保信息的结构化数据完整

验证项（需求实例化页面）：
- [ ] 是否成功提取了"画场景"章节
- [ ] 场景数量是否与原始页面一致
- [ ] 每个场景是否包含 **场景描述** 子章节（格式：`#### **场景描述**`）
- [ ] 每个场景是否包含 **验收准则** 子章节（格式：`#### **验收准则**`）
- [ ] 验收准则表格是否包含 Given|When|Then 三列
- [ ] 表格中的所有单元格内容是否完整提取

验证项（需求方案页面）：
- [ ] 是否成功提取了"性能设计"章节
- [ ] 是否成功提取了"质量属性设计"章节
- [ ] 是否检查了"组件接口说明"章节
- [ ] 如果存在"组件接口说明"章节，是否判断了是否涉及组件接口内容
- [ ] 如果涉及组件接口，是否成功提取了"组件接口说明"章节及其所有子章节
- [ ] 是否检查了"UX设计"章节
- [ ] 如果存在"UX设计"章节，是否判断了是否涉及WEB页面功能内容
- [ ] 如果涉及WEB页面功能，是否成功提取了"UX设计"章节及其所有子章节
- [ ] 章节内容是否完整（无遗漏）
- [ ] 子章节和格式是否保持原样

通用验证项：
- [ ] Markdown 格式是否正确（标题层级、表格格式等）
- [ ] 文件是否成功写入到指定位置
- [ ] 文件内容是否可读且结构清晰

后果：
- 不完整的信息提取会导致后续需求分析错误
- 格式错误会影响文档的可读性和可维护性


## [规则 5] 文件写入和追加策略（优先级：中）
# 处理多个输出文件的写入和追加

说明：
- 根据页面类型和内容，写入到不同的文件：
  - **需求实例化**页面 → `需求画场景.md`
  - **需求方案**页面 → `需求方案-Q.md`（性能设计和质量属性设计）
  - **需求方案**页面（如果存在组件接口说明） → `接口规格.md`
  - **需求方案**页面（如果存在UX设计且涉及WEB页面功能） → `WEB UI规格.md`
- 如果文件不存在，创建新文件
- 如果文件已存在，追加内容并保持风格一致
- 追加时添加分隔符和日期标记，避免内容混淆
- 保持文件整体结构的清晰和可读性

### 需求画场景.md 文件结构：
```markdown
# 需求画场景

> 最后更新：2025-01-XX

## 场景来源：[页面标题或URL]

### 场景1：
#### **场景描述**
[场景描述内容]

#### **验收准则**
|Given|When|Then|
|-|-|-|
|...|...|...|

### 场景2：
#### **场景描述**
[场景描述内容]

#### **验收准则**
|Given|When|Then|
|-|-|-|
|...|...|...|

---

## 场景来源：[另一个页面标题或URL]
...
```

### 需求方案-Q.md 文件结构：
```markdown
# 需求方案-Q（质量属性）

> 最后更新：2025-01-XX

## 来源：[页面标题或URL]

## 性能设计
[性能设计章节的完整内容]

---

## 质量属性设计
[质量属性设计章节的完整内容]

---

## 来源：[另一个页面标题或URL]
...
```

### 接口规格.md 文件结构：
```markdown
# 接口规格

> 最后更新：2025-01-XX

## 来源：[页面标题或URL]

## 组件接口说明
[组件接口说明章节的完整内容，包括所有子章节]

### 子章节1
[子章节内容]

### 子章节2
[子章节内容]

---

## 来源：[另一个页面标题或URL]
...
```

### WEB UI规格.md 文件结构：
```markdown
# WEB UI规格

> 最后更新：2025-01-XX

## 来源：[页面标题或URL]

## UX设计
[UX设计章节的完整内容，包括所有子章节]

### 页面布局设计
[页面布局设计子章节内容]

### 页面交互设计
[页面交互设计子章节内容]

### 页面流程设计
[页面流程设计子章节内容]

---

## 来源：[另一个页面标题或URL]
...
```

追加格式：
- 使用 `---` 分隔不同来源的内容
- 添加来源信息（页面标题或 URL）
- 添加提取时间戳
- 保持与现有内容的格式一致


## [规则 6] 错误处理和用户反馈（优先级：中）
# 提供清晰的错误信息和用户反馈

说明：
- MCP 服务不可用时，明确告知用户
- 提取失败时，提供详细的错误信息
- 成功提取时，告知用户提取的内容数量和文件位置
- 根据页面类型提供不同的反馈信息
- 提供后续操作建议

反馈格式（需求实例化页面）：
- 成功：`成功提取 [N] 个场景，已保存到 需求画场景.md`
- 失败：`提取失败：[具体错误原因]，请检查页面链接或 MCP 服务状态`
- 警告：`检测到 [问题描述]，已处理：[处理方式]`

反馈格式（需求方案页面）：
- 成功：`成功提取性能设计和质量属性设计章节，已保存到 需求方案-Q.md`
- 成功（含接口）：`成功提取性能设计、质量属性设计和组件接口说明章节，已保存到 需求方案-Q.md 和 接口规格.md`
- 成功（含UI）：`成功提取性能设计、质量属性设计和UX设计章节，已保存到 需求方案-Q.md 和 WEB UI规格.md`
- 成功（含接口和UI）：`成功提取性能设计、质量属性设计、组件接口说明和UX设计章节，已保存到 需求方案-Q.md、接口规格.md 和 WEB UI规格.md`
- 接口检测：`检测到组件接口说明章节，已提取接口规格信息，已保存到 接口规格.md`
- 接口未检测：`未检测到组件接口说明章节或章节不涉及接口内容`
- UI检测：`检测到UX设计章节，已提取WEB UI规格信息，已保存到 WEB UI规格.md`
- UI未检测：`未检测到UX设计章节或章节不涉及WEB页面功能内容`
- 失败：`提取失败：[具体错误原因]，请检查页面链接或 MCP 服务状态`
- 警告：`检测到 [问题描述]，已处理：[处理方式]`

通用反馈：
- 页面类型识别：`检测到 [需求实例化/需求方案] 页面`
- 章节提取：`开始提取 [章节名称] 章节`


# ============================================
# 摘要 - 关键规则（最高优先级）
# ============================================

1. ✅ [规则 1] 必须使用本地 icenter-markdown MCP 服务提取页面信息（关键）
2. ✅ [规则 2] 需求实例化页面：提取"画场景"章节，写入需求画场景.md（关键）
3. ✅ [规则 3] 需求方案页面：提取"性能设计"和"质量属性设计"章节，写入需求方案-Q.md（关键）
4. ✅ [规则 3.1] 需求方案页面：提取"组件接口说明"章节，写入接口规格.md（关键）
5. ✅ [规则 3.2] 需求方案页面：提取"UX设计"章节，写入WEB UI规格.md（关键）
6. ✅ [规则 4] 验证信息提取的完整性（高）
7. ✅ [规则 5] 正确处理文件写入和追加（中）
8. ✅ [规则 6] 提供清晰的错误处理和用户反馈（中）


# ============================================
# 使用示例
# ============================================

示例 1：提取需求实例化页面
```
用户输入：
"请从 https://example.com/requirement-instance 提取需求信息"

AI 处理：
1. 调用 icenter-markdown MCP 服务提取页面内容
2. 检测到"需求实例化"关键字
3. 输出："检测到需求实例化页面，开始提取'画场景'章节"
4. 提取所有场景信息，确保每个场景包含：
   - ### 场景N：标题
   - #### **场景描述** 内容
   - #### **验收准则** 表格（Given|When|Then）
5. 验证提取完整性（场景数量、格式、表格内容）
6. 写入到 需求画场景.md
7. 输出："成功提取 5 个场景，已保存到 需求画场景.md"
```

示例 2：提取需求方案页面（不含组件接口说明）
```
用户输入：
"请从 https://example.com/requirement-solution 提取需求信息"

AI 处理：
1. 调用 icenter-markdown MCP 服务提取页面内容
2. 检测到"需求方案"关键字
3. 输出："检测到需求方案页面，开始提取'性能设计'和'质量属性设计'章节"
4. 检查是否存在"组件接口说明"章节
5. 未检测到"组件接口说明"章节或章节不涉及接口内容
6. 提取"性能设计"章节的所有内容
7. 提取"质量属性设计"章节的所有内容
8. 验证提取完整性（章节完整性、格式）
9. 写入到 需求方案-Q.md
10. 输出："成功提取性能设计和质量属性设计章节，已保存到 需求方案-Q.md"
11. 输出："未检测到组件接口说明章节或章节不涉及接口内容"
```

示例 2.1：提取需求方案页面（含组件接口说明）
```
用户输入：
"请从 https://example.com/requirement-solution 提取需求信息"

AI 处理：
1. 调用 icenter-markdown MCP 服务提取页面内容
2. 检测到"需求方案"关键字
3. 输出："检测到需求方案页面，开始提取'性能设计'、'质量属性设计'、'组件接口说明'和'UX设计'章节"
4. 检查是否存在"组件接口说明"章节
5. 检测到"组件接口说明"章节，分析内容确认涉及组件接口
6. 输出："检测到组件接口说明章节，将提取接口规格信息"
7. 检查是否存在"UX设计"章节
8. 未检测到"UX设计"章节或章节不涉及WEB页面功能
9. 提取"性能设计"章节的所有内容
10. 提取"质量属性设计"章节的所有内容
11. 提取"组件接口说明"章节及其所有子章节的所有内容
12. 验证提取完整性（章节完整性、格式）
13. 写入到 需求方案-Q.md（性能设计和质量属性设计）
14. 写入到 接口规格.md（组件接口说明）
15. 输出："成功提取性能设计、质量属性设计和组件接口说明章节，已保存到 需求方案-Q.md 和 接口规格.md"
16. 输出："未检测到UX设计章节或章节不涉及WEB页面功能内容"
```

示例 2.2：提取需求方案页面（含UX设计）
```
用户输入：
"请从 https://example.com/requirement-solution 提取需求信息"

AI 处理：
1. 调用 icenter-markdown MCP 服务提取页面内容
2. 检测到"需求方案"关键字
3. 输出："检测到需求方案页面，开始提取'性能设计'、'质量属性设计'、'组件接口说明'和'UX设计'章节"
4. 检查是否存在"组件接口说明"章节
5. 未检测到"组件接口说明"章节或章节不涉及接口内容
6. 检查是否存在"UX设计"章节
7. 检测到"UX设计"章节，分析内容确认涉及WEB页面功能
8. 输出："检测到UX设计章节，将提取WEB UI规格信息"
9. 提取"性能设计"章节的所有内容
10. 提取"质量属性设计"章节的所有内容
11. 提取"UX设计"章节及其所有子章节的所有内容
12. 验证提取完整性（章节完整性、格式）
13. 写入到 需求方案-Q.md（性能设计和质量属性设计）
14. 写入到 WEB UI规格.md（UX设计）
15. 输出："成功提取性能设计、质量属性设计和UX设计章节，已保存到 需求方案-Q.md 和 WEB UI规格.md"
16. 输出："未检测到组件接口说明章节或章节不涉及接口内容"
```

示例 2.3：提取需求方案页面（含组件接口说明和UX设计）
```
用户输入：
"请从 https://example.com/requirement-solution 提取需求信息"

AI 处理：
1. 调用 icenter-markdown MCP 服务提取页面内容
2. 检测到"需求方案"关键字
3. 输出："检测到需求方案页面，开始提取'性能设计'、'质量属性设计'、'组件接口说明'和'UX设计'章节"
4. 检查是否存在"组件接口说明"章节
5. 检测到"组件接口说明"章节，分析内容确认涉及组件接口
6. 输出："检测到组件接口说明章节，将提取接口规格信息"
7. 检查是否存在"UX设计"章节
8. 检测到"UX设计"章节，分析内容确认涉及WEB页面功能
9. 输出："检测到UX设计章节，将提取WEB UI规格信息"
10. 提取"性能设计"章节的所有内容
11. 提取"质量属性设计"章节的所有内容
12. 提取"组件接口说明"章节及其所有子章节的所有内容
13. 提取"UX设计"章节及其所有子章节的所有内容
14. 验证提取完整性（章节完整性、格式）
15. 写入到 需求方案-Q.md（性能设计和质量属性设计）
16. 写入到 接口规格.md（组件接口说明）
17. 写入到 WEB UI规格.md（UX设计）
18. 输出："成功提取性能设计、质量属性设计、组件接口说明和UX设计章节，已保存到 需求方案-Q.md、接口规格.md 和 WEB UI规格.md"
```

示例 3：MCP 服务不可用
```
用户输入：
"请从 https://example.com/requirement-page 提取需求信息"

AI 处理：
1. 检查 icenter-markdown MCP 服务
2. 服务不可用
3. 输出："icenter-markdown MCP 服务当前不可用，请确认服务状态后再试"
4. 不执行其他提取方式
```

示例 4：追加场景到现有文件（需求实例化）
```
用户输入：
"请从 https://example.com/another-requirement-instance 提取需求信息"

AI 处理：
1. 调用 MCP 服务提取内容
2. 检测到需求实例化页面
3. 提取场景信息，确保格式符合标准：
   - ### 场景N：
   - #### **场景描述**
   - #### **验收准则** 表格
4. 检查 需求画场景.md 已存在
5. 追加新场景，添加分隔符和来源信息
6. 保持格式一致（场景编号、场景描述、验收准则表格）
7. 输出："成功提取 3 个场景，已追加到 需求画场景.md"
```

示例 5：追加内容到现有文件（需求方案）
```
用户输入：
"请从 https://example.com/another-requirement-solution 提取需求信息"

AI 处理：
1. 调用 MCP 服务提取内容
2. 检测到需求方案页面
3. 提取"性能设计"和"质量属性设计"章节
4. 检查 需求方案-Q.md 已存在
5. 追加新内容，添加分隔符和来源信息
6. 保持格式一致
7. 输出："成功提取性能设计和质量属性设计章节，已追加到 需求方案-Q.md"
```


# ============================================
# 场景格式说明
# ============================================

"画场景"章节的标准格式：

```markdown
### 场景1：
#### **场景描述**
正常登录场景正常登录流程验证，用xxxx支持正常解密。

#### **验收准则**
|Given|When|Then|
|-|-|-|
|xxxxx|xxxxx|xxxxx|
```

关键要求：
- 场景标题使用 H3 级别（`### 场景N：`）
- 场景描述使用 H4 级别（`#### **场景描述**`）
- 验收准则使用 H4 级别（`#### **验收准则**`）
- 验收准则必须是表格格式，包含 Given|When|Then 三列
- 必须完整提取表格中的所有单元格内容


# ============================================
# 页面类型和输出文件对应关系
# ============================================

## 页面类型识别

| 页面类型 | 识别关键字 | 提取章节 | 输出文件 | 用途 |
|---------|----------|---------|---------|------|
| 需求实例化 | "需求实例化" | 画场景 | 需求画场景.md | 生成 MFQ-M（单功能）和 MFQ-F（功能交互）测试用例 |
| 需求方案 | "需求方案" | 性能设计<br>质量属性设计 | 需求方案-Q.md | 生成 MFQ-Q（质量属性）测试用例 |
| 需求方案 | "需求方案" | 组件接口说明<br>（如果存在且涉及接口） | 接口规格.md | 接口设计和开发参考 |
| 需求方案 | "需求方案" | UX设计<br>（如果存在且涉及WEB页面功能） | WEB UI规格.md | WEB页面UI设计和开发参考 |

## 工作流程

```
用户输入页面链接
    │
    ├─→ 调用 icenter-markdown MCP 服务
    │
    ├─→ 识别页面类型
    │
    ├─→ 需求实例化页面
    │   └─→ 提取"画场景"章节
    │       └─→ 写入 需求画场景.md
    │
    └─→ 需求方案页面
        ├─→ 提取"性能设计"和"质量属性设计"章节
        │   └─→ 写入 需求方案-Q.md
        │
        ├─→ 检查"组件接口说明"章节
        │   └─→ 如果存在且涉及接口内容
        │       └─→ 提取"组件接口说明"章节及其所有子章节
        │           └─→ 写入 接口规格.md
        │
        └─→ 检查"UX设计"章节
            └─→ 如果存在且涉及WEB页面功能内容
                └─→ 提取"UX设计"章节及其所有子章节
                    └─→ 写入 WEB UI规格.md
```


# ============================================
# 版本历史
# ============================================
# v1.4 (2025-01-XX) - 新增UX设计章节提取，如果存在且涉及WEB页面功能内容，生成WEB UI规格.md
# v1.3 (2025-01-XX) - 新增组件接口说明章节提取，如果存在且涉及接口内容，生成接口规格.md
# v1.2 (2025-01-XX) - 新增需求方案页面支持，提取性能设计和质量属性设计章节，输出到需求方案-Q.md
# v1.1 (2025-01-XX) - 更新场景格式说明，明确场景描述和验收准则的标准格式
# v1.0 (2025-01-XX) - 初始版本，定义需求提取的核心规则
# ============================================
